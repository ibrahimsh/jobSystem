package com.example.jobsystem;

public class queries {
    /**
     * this  is  query  for  creating  users  table
     */
    public static String q1 = "Create table  IF Not exists user(name varchar(200),id bigint)";
}
